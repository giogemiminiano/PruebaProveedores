spring:

  datasource:
    url: jdbc:h2:mem:supplierDB
    username: chapur
    password: chapur
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: validate

    properties:
      hibernate:
        format_sql: true

    show-sql: true

  flyway:
    enabled: true

server:
  port: 8080